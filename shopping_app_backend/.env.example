# Backend environment variables
# Prefer setting DATABASE_URL directly.
# If you only have db_connection.txt's line (e.g., 'psql postgresql://...'), you can put it into DB_CONNECTION_TXT.
DATABASE_URL=postgresql+psycopg://appuser:dbuser123@localhost:5000/myapp
DB_CONNECTION_TXT=psql postgresql://appuser:dbuser123@localhost:5000/myapp

# SECURITY (set this in any non-dev environment)
JWT_SECRET_KEY=change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXP_MINUTES=60

# CORS: set to your frontend origin (comma-separated for multiple); use * only in development
CORS_ALLOW_ORIGINS=http://localhost:3000
